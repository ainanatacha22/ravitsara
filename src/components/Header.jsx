import React from 'react'
import { WrapperComponents } from '../hoc'
import "../styles/Header.css"

const Header = () => {
    return (
        <div className='bgheader'>
        </div>
    )
}

export default WrapperComponents(Header, "")
